{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "echo 'claudeslop runs TypeScript via tsx; no build step'"
  },
  "deploy": {
    "startCommand": "pnpm --filter claudeslop-labeler start",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
