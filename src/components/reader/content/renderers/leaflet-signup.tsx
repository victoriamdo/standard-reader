"use client";

/**
 * `pub.leaflet.blocks.signup` is a publication-specific newsletter signup CTA
 * with no reader-relevant content. We intentionally render nothing.
 */
export function LeafletSignupBlockView() {
  return null;
}
